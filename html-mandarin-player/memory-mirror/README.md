## Memory mirror — html-mandarin-player

**Last synced from Drive:** 2026-07-27

**This is a fallback copy, not the primary source.** Primary memory for this app lives at
`I:\My Drive\WS Fam\Memory\html-player\` on Google Drive — see the repo root
[README.md](../../README.md) for the full primary-vs-fallback rule. Only rely on this mirror if
that Drive path is genuinely unreachable, and sync any changes made here back to Drive once it's
reachable again.

This folder is a periodic manual checkpoint (not auto-synced on every change) — content here may
lag behind the live Drive copy by however long it's been since the last checkpoint.
